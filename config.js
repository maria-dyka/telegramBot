// set your keys as environment variables
const API_KEY = process.env.API_KEY_MUSIC,
      token = process.env.TOKEN_TELEGRAM_WILD_BOT;

let config = {
    API_KEY: API_KEY,
    TOKEN: token
};

module.exports = config;
